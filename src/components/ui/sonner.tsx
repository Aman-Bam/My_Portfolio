import React from 'react';
import { Toaster as SonnerToaster } from 'sonner';

// Re-export sonner's Toaster so the app can import it as a named export
export const Toaster = SonnerToaster;

export default Toaster;
